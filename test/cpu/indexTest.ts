
import { expect } from 'chai'
import CPU from '../../src/cpu'

describe('CPU', () => {
    describe('running', () => {
        it('should work', () => {
            const cpu = new CPU([])
        })
    })
})